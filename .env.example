# Database Credentials
POSTGRES_PASSWORD=password
POSTGRES_DB=nmap_db
POSTGRES_USER=user

# Application Connection String
# Inside Docker, use 'db'. Outside Docker (host), use 'localhost'.
DATABASE_URL=postgresql+asyncpg://user:password@db:5432/nmap_db